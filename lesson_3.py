# Lesson 2 - Строки в Python
#  #TODO - Нам осталось разобрать многострочные форматированные строки, сырые строки и проблемы в Windows с обратным слэшем в путях, а также примеры их решения.

# Многострочные строки
multi_line_string = """Это многострочная строка.
Она может занимать несколько строк.
     И сохранять все отступы и переносы строк."""

# Форматированные строки (f-strings)
name = "Боб"
age = 30
multiline_f_string = f"""Привет, 
меня зовут {name}.
Мне {age} лет.
Я люблю программировать на Python."""

# Метод формат позволяет форматировать строки, используя плейсхолдеры
# formatted_string = "Привет, меня зовут {}. Мне {} лет."
# formatted_string = "Привет, меня зовут {name}. Мне {age} лет."
# name = "Алиса"
# age = 25
# formatted_string = formatted_string.format(name, age) # TODO - Можно ли так?
# formatted_string = formatted_string.format(name=name, age=age)

# path = "C:\Users\nikolay\Desktop"
# print(path)
path = "C:\\Users\\nikolay\\Desktop"
print(path)
path = r"C:\Users\nikolay\Desktop"
print(path)

user_name = "tatyana"
home_dir = rf"C:\Users\{user_name}\Desktop"

# Методы строк
# len() — возвращает длину строки - функция
# in - оператор проверки наличия подстроки в строке
# .split() — разбивает строку на список подстрок по разделителю
# .strip() — удаляет пробельные символы в начале и в конце строки
# .join() — объединяет элементы списка в одну строку
# .replace() — заменяет одну подстроку на другую
# .lower() — переводит строку в нижний регистр
# .upper() — переводит строку в верхний регистр
# .count() — возвращает количество вхождений подстроки
# .format() — форматирование строк (старый стиль)
# .find() — поиск индекса первого вхождения подстроки
# .startswith() — проверяет, начинается ли строка с указанного значения
# .endswith() — проверяет, заканчивается ли строка указанным значением
# .capitalize() — делает первую букву строки заглавной
# .title() — делает заглавной первую букву каждого слова
# .isdigit() — проверяет, состоит ли строка только из цифр
# .isalpha() — проверяет, состоит ли строка только из букв
# .isalnum() — проверяет, состоит ли строка только из букв и цифр
# .isupper() — проверяет, состоит ли строка только из заглавных букв
# .islower() — проверяет, состоит ли строка только из строчных букв
# .isspace() — проверяет, состоит ли строка только из пробельных символов
# .rfind() — поиск индекса последнего вхождения подстроки
# .index() — поиск индекса подстроки (вызывает ошибку, если не найдено)
# .lstrip() — удаляет пробелы только слева
# .rstrip() — удаляет пробелы только справа
# .partition() — разбивает строку на три части по разделителю
# .f-strings — (механизм) современное форматирование строк с префиксом f


# Строка - упорядоченная неизменяемая коллекция символов
# Нумерация с 0

string = "Мама мыла раму"
print(id(string))

# string[0] = "П"  # TypeError: 'str' object does not support item assignment
# string[0] = "П"

string = string.replace("мыла", "ела")
print(string)
print(id(string))

string = "Мама мыла раму"
new_string = string.replace("мыла", "пила").replace("раму", "колу").replace("раму", "доброколу")
print(new_string)

# PRACTICE - Ввод данных и замена через метод формат
"""
1.
Запросите у пользователя его имя, фамилию и возраст. Затем выведите строку вида "Привет, меня зовут <имя> <фамилия>. Мне <возраст> лет." Используйте метод format для форматирования строки или форматированную строку.
Выведите данные принтом
2. Замените методом replace имя и фамилию на другие.
Выведите данные принтом
"""

name = input("Введите ваше имя: ")
last_name = input("Введите вашу фамилию: ")
age = input("Введите ваш возраст: ")

result = f"Привет, меня зовут {name} {last_name}. Мне {age} лет."
print(result)

# Часть 2
new_name = "Иван"
new_last_name = "Петров"

result2 = result.replace(name, new_name).replace(last_name, new_last_name)
print(result2)

# СРЕЗЫ СТРОК
# SSS - Start Stop Step
# [start:stop:step]
# Индексация начинается с нуля

string = "Три закона робототехники"

print(string[::]) # Тут использовались стандартные значения: строка с самого начала по самый конец, каждую первую букву.``
print(string[5::]) 
print(string[5:])
# По пятый
print(string[:5:])
print(string[:5])

print(string[::2])
print(string[5:10:2])
print(string[::-1])
print(string[::-2])

# Палиндром - слово которое читается одинаково в обе стороны

# Довод == довоД
# Заказ
# Казак
# Радар
# Шалаш
# Ротор
# Топот
# А роза упала на лапу Азора
# Аргентина манит негра
# Кони, топот, инок
# Искать такси
# Около Миши молоко

word = "Около Миши молоко"
raw_word = word.lower().replace(" ", "").replace(",", "").replace(".", "")
result_check = raw_word == raw_word[::-1]
raport_string = f"Слово '{word}'  палиндромом: {result_check}"
print(raport_string)


# Встроенная функция в Python `input()` всегда возвращает строку. Даже если пользователь ничего не ввёл, она вернёт пустую строку.
# TypeError: can only concatenate str (not "int") to str
# Строки складываются только со строками. Числа только с числами.
# Для конвертации мы можем исопльзвать int
age = int(input("Введите возраст: "))
print(f"Через год ваш возраст: {age + 1}")
# print(age)

pi = 3,14
print(pi)
print(type(pi))
print(len(pi))

pi = 3.14
print(pi)
print(type(pi))
# print(len(pi)) # TypeError: object of type 'float' has no len()

my_salary_per_second = 3_000_000_000